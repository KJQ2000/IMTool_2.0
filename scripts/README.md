# Scripts Layout

- `scripts/verification/`: repeatable validation helpers and smoke tests.
- `scripts/scratch/`: one-off or experimental utility scripts that are not part of normal validation.

Keeping these separate makes it easier to find the scripts that are safe to rerun during maintenance.
